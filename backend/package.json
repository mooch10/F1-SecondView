{
  "name": "rebufo-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc && node -e \"const fs=require('fs'); fs.mkdirSync('dist/data', {recursive:true}); fs.copyFileSync('src/data/radio_transcripts.json', 'dist/data/radio_transcripts.json');\"",
    "start": "node dist/index.js",
    "check": "biome check src",
    "format": "biome format --write src"
  },
  "dependencies": {
    "typescript": "^5.7.3"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "^22.13.4",
    "tsx": "^4.19.3"
  }
}
